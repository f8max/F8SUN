# Prototype sources

- `concept_sources/C1` — primary full-site baseline.
- `concept_sources/C3` — Ride Replay/state-machine module.
- `concept_sources/C4` — editable frame brief/export module.
- `visual_reference/C1-C5-overview.jpg` — all five concepts.
- Full C1/C3/C4 captures are retained for visual parity.

## Run

```powershell
cd concept_sources\C1
npm ci
npm run dev
```

Replace `C1` with `C3` or `C4`.

Sources are exact lean extractions from the supplied archives; generated evidence and builds
are omitted. Prototype product copy is not source-of-truth.
